#include "Vector2.h"

/**
 * constructor with no input
 * (0.0, 0.0)
 */
Vector2::Vector2() {
	x = 0.0;
	y = 0.0;
}

/**
 * constructor with init
 * (x, y)
 */
Vector2::Vector2(float x, float y) {
	x = x;
	y = y;
}
